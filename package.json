{
  "private": true,
  "scripts": {
    "re:watch": "bsb -make-world",
    "re:build": "bsb -make-world -clean-world",
    "pa:watch": "parcel public/index.html",
    "pa:build": "parcel build public/index.html",
    "watch": "yarn re:watch & yarn pa:watch",
    "build": "yarn re:build && yarn pa:build && yarn shx cp public/robots.txt dist"
  },
  "dependencies": {
    "bs-css-emotion": "^1.1.0",
    "date-fns": "^2.11.0",
    "normalize.css": "^8.0.1",
    "reason-date-fns": "^0.10.4",
    "reason-react": "^0.7.0",
    "typeface-exo-2": "^1.1.4"
  },
  "devDependencies": {
    "bs-platform": "^7.2.2",
    "parcel-bundler": "^1.12.4",
    "shx": "^0.3.2"
  }
}
